// input: several lines
// each line has a number or blank
// blank indicates end of group
// for each group, calculate sum of numbers in their lines
// output: max amount in a group
use std::str;


pub fn compute_day_one_answer(contents: String) {
    let lines = str::lines(contents);
		// println!("{lines}")
		// ()
		("1")
}
